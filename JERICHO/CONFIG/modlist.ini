# JERICHO module list — load order and enabled state.
#
# This file is the single source of truth for which modules run and in
# which order. Line order = module load order. Enabled values: 1/0
# (true/false/enabled/disabled are also accepted). A module not listed
# here follows its default-enabled flag from JERICHO/MODS/<id>/mod.toml.
# The frontend (Options -> JERICHO) rewrites this file on toggle/reorder.
crumple = 1
d2pl = 1
levelhacks = 1
sandbox = 0
example = 0
